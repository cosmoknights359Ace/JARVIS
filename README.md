Hey! This is an ollama based chatbot which uses models qwen coder 2.5 3b and 7b.
The theme lies around iron man's Jarvis
It can do things like Analysing images,store chat memory,convos,generate code,stuff
This model can run entirely offline,since the models are locally installed and run.
